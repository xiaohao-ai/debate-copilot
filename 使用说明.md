# 反方辩论作战台・使用说明（豆包流式识别版）

本工具是一个**本地运行**的辩论辅助网页，包含：实时语音转写、对方话术自动匹配反驳、我方各环节逐字稿、话术库、豆包陪练采集。

识别引擎有两种，可在页面左上角随时切换：



* **浏览器自带（免配置）**：零配置，但在国内网络下识别服务可能不稳定；

* **豆包流式识别（本地代理）**：中文识别更准、国内稳定、支持辩论术语热词。需要按下面三步做一次性准备。



***

## 一、一次性准备（约 10 分钟）

### 第 1 步：开通服务并拿到密钥



1. 用电脑浏览器打开火山引擎官网 [https://www.volcengine.com/](https://www.volcengine.com/) ，注册 / 登录，完成实名认证（个人即可）。

2. 进入「豆包语音 / 语音技术」控制台，开通**大模型流式语音识别（豆包流式语音识别模型 2.0）**，新账号有免费试用额度，需在控制台手动领取 / 开通。

3. 打开新版控制台「API Key 管理」页，创建一个 **X-Api-Key** 并复制保存。

* 页面路径参考：控制台 → 语音技术 → 设置 → API Key 管理。

* 计费为按时长（约 2.4–3.5 元 / 小时），一场辩论加练习的费用可忽略，注意比赛后停止服务即可。

### 第 2 步：把密钥写进本机配置



1. 在本文件夹中找到 `config.example.json`，复制一份并改名为 `config.json`（首次运行 `node server.js` 时也会自动生成）。

2. 用文本编辑器打开 `config.json`，把密钥粘贴到 `apiKey` 引号里：



```
{ "apiKey": "把你的 X-Api-Key 粘贴到这里", "engine": "seedasr2" }
```

> `config.json`
>
>  只保存在你自己的电脑上、只发给火山引擎做鉴权，
>
> **不要把它发给别人或传到网上**
>
> 。
>
> `hotwords`
>
>  是识别热词，可自行增删辩论术语。

### 第 2.5 步（可选）：接入豆包大模型，开启「AI 现场生成」

语音识别只负责把对方的话转成文字；右侧「AI 现场生成」（快反 / 按辩位成稿 / 继续延展）需要再配一个**火山方舟**的大模型密钥，注意它和第 1 步的语音密钥**不是同一个**，但属于同一个火山账号。

1. 打开火山方舟控制台 `https://console.volcengine.com/ark`，左下角「API Key 管理」→「创建 API Key」→ 复制（形如 `xxxxxxxx-xxxx-...`）。
2. 打开 `config.json`，把它粘到 `arkApiKey` 的引号里；`arkModel` 已默认填好，一般不用改（想换模型再改这里）。
3. 重启 `node server.js`，启动横幅出现「豆包大模型(AI生成)：已配置」即成功；页面右侧会显示「已接入 · 模型名」。
4. 顶部「我的辩位」选定后，AI 会按该辩位的环节时长生成对应字数与结构的稿件（语速按约 240 字/分钟）；没配此密钥时，预设反驳卡 R01–R10、逐字稿等所有离线功能照常可用。

> 计费：方舟按 token 计费，一场辩论的生成量通常不到一元；模型/额度以方舟控制台为准。密钥同样只留在本机。

### 第 3 步：启动本地服务

1. 安装 Node.js（18 或以上版本；你的电脑已安装 v22，可跳过）。

2. 在本文件夹打开「终端」：

* macOS：在文件夹上右键 →「服务 → 新建位于文件夹位置的终端窗口」；或打开「终端」后输入 `cd ` 再把文件夹拖进窗口，回车。

1. 输入下面的命令并回车（**不需要 npm install，本服务零第三方依赖**）：



```
node server.js
```

看到「本地代理已启动」提示即成功。**比赛当天先启动这个黑窗口，比赛结束后按 Ctrl + C 关闭。**



***

## 二、比赛当天怎么用



1. 保持 `node server.js` 窗口开着。

2. 用 **Chrome 或 Edge** 打开 `http://127.0.0.1:8787`（不要再用双击文件的 file:// 方式）。

3. 「听对方」页 → 识别引擎选「**豆包流式识别**」→ 点「开始收音」→ 浏览器询问麦克风时选「允许」。

4. 对方发言时页面实时出字，并自动弹出我方反驳话术；点「照着念」可朗读、「复制反驳」备用。

5. 顶部先选「我的辩位」（一辩到十辩，选定后自动记住，计时器也会对齐该辩位时长）。右侧「AI 现场生成」三个按钮：
   - **快反·约30秒**：现场插话用，3–8 秒出 120–160 字；
   - **按我辩位成稿**：按该辩位环节时长写完整稿（如二辩 6 分钟约 1440 字，分段标注建议秒数，宁多勿少），适合赛前或间隙生成、扫一遍再念；
   - **继续延展90秒**：没讲够时换一个论证层次补一段，不重复前文。
   下方进度条显示「已生成字数 / 预计时长 / 目标时长」。

5. 「我发言」页按环节看逐字稿，顶部有环节计时器；「话术库」可检索；「陪练」页是豆包 App「一辩高下」对练流程。

### 如果现场不让开电脑 / 网络不稳



* 识别引擎切回「浏览器自带」；或用手机讯飞输入法语音、系统听写、豆包 / 飞书实时字幕得到文字，粘贴到「手动分析」，反驳匹配在本机离线运行，完全不受影响。



***

## 三、常见问题



| 现象                   | 原因与处理                                                                               |
| -------------------- | ----------------------------------------------------------------------------------- |
| 点开始收音提示「未填写密钥」       | `config.json` 的 `apiKey` 为空，填好后**重启** `node server.js`                              |
| 火山返回 401 / 鉴权错误      | 密钥填错、或服务未开通；核对控制台，注意 X-Api-Key 不要带空格                                                |
| 终端/页面报 403 requested resource not granted | 服务没在同一项目开通：控制台左上角切到 Default 默认项目 →「开通管理」→「流式语音识别 2.0」点开通/试用（赠 20 小时），等 1–3 分钟生效 |
| 浏览器提示没有麦克风权限         | 地址栏左侧图标 → 网站设置 → 麦克风 → 允许；macOS 还要在「系统设置 → 隐私与安全性 → 麦克风」给浏览器授权                      |
| 页面打不开 127.0.0.1:8787 | 本地服务没启动；回到终端确认 `node server.js` 在运行                                                 |
| 一直不出字                | 看终端是否报错；确认用的是 [http://127.0.0.1:8787](http://127.0.0.1:8787) 而不是文件方式；换「浏览器自带」引擎对照排查 |
| 识别有错别字               | 把术语加进 `config.json` 的 `hotwords`，保存后在页面重新点「开始收音」                                    |
| AI 生成一直显示「未接入」 | `config.json` 的 `arkApiKey` 为空或重启前未生效；填好方舟密钥后**重启** `node server.js`，刷新页面 |
| AI 生成报 ark-401 / ark-404 | 401：方舟密钥错或带了空格；404：`arkModel` 模型名你的账号不可用，到方舟「在线推理」里换一个可用模型 ID 填进 `arkModel` |
| AI 出字慢/长稿要等         | 快反约 3–8 秒；6 分钟长稿约 20–40 秒，属正常，长稿请在赛前或间隙提前生成，不要等上台才点 |



***

## 四、文件说明



* `index.html`：工具页面（全部界面与话术内容）

* `server.js`：本地代理（托管页面 + 转发语音到火山 ASR + 转发 AI 生成请求到火山方舟），零第三方依赖

* `config.example.json`：配置模板

* `config.json`：你的本机配置（含密钥，勿外发）